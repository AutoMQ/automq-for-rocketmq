<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one or more
  ~ contributor license agreements.  See the NOTICE file distributed with
  ~ this work for additional information regarding copyright ownership.
  ~ The ASF licenses this file to You under the Apache License, Version 2.0
  ~ (the "License"); you may not use this file except in compliance with
  ~ the License.  You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.automq.rocketmq.controller.metadata.database.mapper.S3ObjectMapper">

    <insert id="create" parameterType="S3Object" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO s3object (object_id, object_size, stream_id, state)
        VALUES (
            #{objectId},
            #{objectSize},
            #{streamId},
            #{state}
        )
    </insert>

    <select id="getById" resultType="S3Object">
        SELECT id, object_id, stream_id, object_size, prepared_timestamp, committed_timestamp, expired_timestamp,
        marked_for_deletion_timestamp, state
        FROM s3object
        WHERE id = #{id}
    </select>

    <select id="getByObjectId" resultType="S3Object">
        SELECT id, object_id, stream_id, object_size, prepared_timestamp, committed_timestamp, expired_timestamp,
        marked_for_deletion_timestamp, state
        FROM s3object
        WHERE object_id = #{objectId}
    </select>

    <update id="delete" parameterType="S3Object">
        UPDATE s3object
        SET marked_for_deletion_timestamp = #{markedForDeletionTimestamp}
        WHERE 1 = 1
        <if test="null != id">
            AND id = #{id}
        </if>
        <if test="null != objectId">
            AND object_id = #{objectId}
        </if>
    </update>

    <update id="deleteDangerous">
        DELETE FROM s3object
        WHERE 1 = 1
    </update>

    <update id="prepare" parameterType="S3Object">
        UPDATE s3object
        SET prepared_timestamp = #{preparedTimestamp}, expired_timestamp = #{expiredTimestamp}
        WHERE 1 = 1
        <if test="null != id">
            AND id = #{id}
        </if>
        <if test="null != objectId">
            AND object_id = #{objectId}
        </if>
    </update>

    <update id="expired" parameterType="S3Object">
        UPDATE s3object
        SET expired_timestamp = #{expiredTimestamp}
        WHERE 1 = 1
        <if test="null != id">
            AND id = #{id}
        </if>
        <if test="null != objectId">
            AND object_id = #{objectId}
        </if>
    </update>

    <update id="commit" parameterType="S3Object">
        UPDATE s3object
        SET committed_timestamp = #{committedTimestamp}
        WHERE 1 = 1
        <if test="null != id">
            AND id = #{id}
        </if>
        <if test="null != objectId">
            AND object_id = #{objectId}
        </if>
    </update>

    <select id="list" resultType="S3Object">
        SELECT id, object_id, stream_id, object_size, prepared_timestamp, committed_timestamp, expired_timestamp,
        marked_for_deletion_timestamp, state
        FROM s3object
        WHERE 1 = 1
        <if test="null != state">
            AND state = #{state}
        </if>
        <if test="null != expiredTimestamp">
            AND expired_timestamp &lt;= #{expiredTimestamp}
        </if>
    </select>
    
</mapper>